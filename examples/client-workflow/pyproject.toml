[project]
name = "client-workflow"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "bnbagent",
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
bnbagent = { path = "../..", editable = true }
